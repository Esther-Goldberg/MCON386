namespace MCON368.WinForms.WinForms
{
    public partial class LoginForm : Form
    {
        public LoginForm()
        {
            InitializeComponent();
            loginPanel.BackColor = Color.FromArgb(20, Control.DefaultBackColor);
        }
    }
}
